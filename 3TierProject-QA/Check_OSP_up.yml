---
# Use Jumpbox to check that OpenStack instances are up
- hosts: jumpbox
  gather_facts: no
  tasks:
  - name: Gather facts
    include_role: 
       name: osp.facts

- name: ping
  hosts: all
  gather_facts: no
  become: true
  tasks:
  - ping:
